---
description: Составить план, заточенный под Trello — декомпозиция на карточки с чек-листами и зависимостями для @task-manager
agent: evol-plan
---

Ты выполняешься как агент `evol-plan` (subagent, контекст не засоряешь).
Составь план из текста пользователя, который сразу превращается в Trello-карточки.

Текст задачи: `$ARGUMENTS` (может быть пустым — тогда выясни задачу вопросом, а не придумывай).

Действуй строго по `skills/evol-plan/SKILL.md`: разведка репо + `init.sh`/`boards.sh`/`lists.sh`/`dump.sh` (точные имена, не выдумывай) → декомпозиция на 3–7 карточек по строгому формату `## Контекст/Что сделать/Критерии/Связи` + чек-листы vs отдельные карточки + `Blocked by:` только на реальные URL → выведи два блока: markdown для человека и JSON для `@task-manager`.

Правила: только чтение и планирование, карточки не создаёшь (`edit: deny`, нет `create.sh`); Trello API — только скрипты; неполный план не выдаёшь — уточняешь вопросом.

После плана подскажи: «Передай JSON @task-manager — он заведёт карточки через create.sh/checklist.sh».
