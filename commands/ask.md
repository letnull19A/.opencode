---
description: Только ответить на вопрос — без правок кода и без запусков
agent: ask
---

Ты выполняешься как агент `ask` (readonly). Ответь на вопрос пользователя, ничего не меняя.

Вопрос: `$ARGUMENTS`

Правила: только чтение (`read`/`grep`/`glob`/`context7`), без `edit`/`bash`/`task`; если нужен факт — прочитай файл и укажи `path:line`; если просят действие — подскажи команду/агента, не делай сам.
